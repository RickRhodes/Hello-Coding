{
  "name": "azure-document-uploader",
  "version": "1.0.0",
  "description": "A modern web application for uploading documents to Azure Storage containers",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd server && npm run dev",
    "client:dev": "cd client && npm start",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["azure", "storage", "upload", "documents", "react", "nodejs"],
  "author": "Azure Document Uploader",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}